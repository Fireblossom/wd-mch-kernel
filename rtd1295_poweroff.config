# Coolboot poweroff via ACPU IPC (added 2026-08-09). Without this the
# kernel has no pm_power_off and "poweroff" halts with power still on;
# the only way to turn the box off was an external switch.
CONFIG_POWER_RESET=y
CONFIG_POWER_RESET_RTD129X=y
